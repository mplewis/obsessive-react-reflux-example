// This is a dinky little config. With this, I don't have to hardcode my
// web endpoints into the backend logic. It's so sparse it could be a JSON file
// instead of a JavaScript file.
module.exports = {
  JSON_POJO_ENDPOINT: 'http://json-pojo-server-99a32bbe-1.mplewis.cont.tutum.io:9980/'
}
